🖼️ ImageCreator
ImageCreator — це високопродуктивний десктопний додаток на Java для пакетної обробки та зміни розміру зображень. Програма використовує багатопотокову архітектуру для швидкої обробки великої кількості файлів та має зручний графічний інтерфейс на базі Java Swing.

Основні можливості
Багатопотоковість: Користувач може самостійно обирати кількість потоків для обробки (від 1 до 16), що дозволяє максимально ефективно використовувати ресурси процесора.

Інтерфейс:

Підтримка Drag & Drop для додавання файлів.

Вставка зображень/файлів з буфера обміну (Ctrl+V).

Індикатор прогресу (Progress Bar) та детальний лог подій.

⚙️ Гнучкі налаштування:

Встановлення точної ширини та висоти (px).

Налаштування префікса для вихідних файлів.

Автоматична нумерація результатів.

Підтримка форматів: JPG, PNG, GIF, BMP.

Технічний стек та Архітектура
Проект реалізовано на Java без використання сторонніх бібліотек. Основна увага приділена роботі з конкурентністю.

Архітектура "Producer-Consumer"
Програма розділяє логіку на два основні етапи для уникнення конфліктів доступу до файлової системи:

Resizing (Pool of Workers):

Використовується ExecutorService (Fixed Thread Pool) для паралельної зміни розміру зображень.

Кожен потік (ImageResizer) обробляє окреме зображення і зберігає тимчасовий файл.

Renaming (Single Consumer Thread):

Використовується потокобезпечна черга BlockingQueue.

Окремий потік (ImageRenamer) забирає оброблені файли з черги та послідовно перейменовує їх згідно з лічильником (AtomicInteger). Це гарантує правильний порядок нумерації файлів (prefix_0001, prefix_0002...), навіть якщо потоки обробки завершують роботу хаотично.

Ключові класи:
ImageCreatorGUI — графічний інтерфейс.

ImageCreator — контролер процесу, ініціалізація пулів потоків.

ImageResizer — логіка масштабування (Callable).

ImageRenamer — логіка перейменування та фіналізації (Runnable).

Інструкція
Клонуйте репозиторій:

Bash

git clone https://github.com/Saenderofle/JAVAMAN2
Відкрийте проект у вашій IDE (IntelliJ IDEA, Eclipse, NetBeans) або скомпілюйте через термінал.

Запустіть головний клас: Файл: Main.java

Використання
Запустіть додаток.

Перетягніть зображення у зону "Drop Zone" або натисніть кнопку "Обрати зображення".

Оберіть папку для збереження результатів ("Обрати папку").

В налаштуваннях вкажіть бажану ширину, висоту та кількість потоків.

Натисніть кнопку "Обробити".

Слідкуйте за логом та статус-баром. Після завершення ви побачите статистику часу виконання.
